cd = Zum Naviegieren 
ls = list (Alles in einen Ordner auflisten lassen) 
cp = zum kopierem für Datein oder Ordner 
mv = um Datein, Ordner zu Verschieben in andere Ordner 
mkdir = mit mkdir kann man Ordner erstellen
rm = um Datein, Ordner zu löschen 
lm = Verknüpfungen erstellen 
tar = Daten, Ordner absichern (.zip) Datein erstellen
dd = Kopiert blockorientierten Konvertieren beliebiger Datein
top = bietet einen blick auf Fortlaufende Prozessaktivitäten in Echtzeit, es zeigt ebenfalls eine Liste der Prozesse, die die meiste CPU-Zeit nutzen und kann eine Schnittstelle zur Manipulation der Prozesse anbieten.
dmesg = Liest die Systemnachrichten dierekt aus dem Kernel-Pufferspeicher und gibt sie als lesbaren Text aus.
less = Ein cmd um aus .txt Datein zu lesen.
vi(m) = Es ist eine weiterentiwicklung des Texteditors vi. 
apt = apt ist ein packing tool
sduo = "Superuser do", es ist die höchste befehlsklasse. 
grep = Ein Programm, welches Filterung nach bestimmten Zeichenketten macht.
cat = Ein Programm, welches den Inhalt von .txt Datein wiedergibt. 
touch = Wird genutzt, um Änderungen und Zugrifftempel von einzelnen Datein oder Verzeichnissen zu ändern.
